<Window x:Class="Spray_Paint_Application.View.EditorWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Spray_Paint_Application.View"
        xmlns:model="clr-namespace:Spray_Paint_Application.Model"
        xmlns:converter="clr-namespace:Spray_Paint_Application.Converter" d:DataContext="{d:DesignInstance Type=local:ColorViewModel}"
        mc:Ignorable="d"
        Title="Spray Paint Application" Height="768" Width="1024" WindowState="Maximized"
        Icon="pack://application:,,,/Resources/spray-paint.ico">

    <Window.Resources>
        <Style x:Key="CircularButton" TargetType="Button">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Ellipse Fill="{TemplateBinding Background}" Stroke="{TemplateBinding BorderBrush}" Width="30" Height="30"/>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>


        <local:ColorViewModel x:Key="ColorViewModel"/>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <DockPanel LastChildFill="True" Grid.Row="0">
            <Menu DockPanel.Dock="Top" Background="LightGray" Padding="5,5">
                <MenuItem Header="File" Padding="10 5" >
                    <MenuItem Header="New" Command="{Binding NewCommand}"/>
                    <MenuItem Header="Open" Command="{Binding OpenCommand}"/>
                    <MenuItem Header="Save As" Command="{Binding SaveCommand}"/>
                </MenuItem>

                <MenuItem Header="View" Padding="10 5" >
                    <MenuItem Header="Zoom In" Command="{Binding ZoomInCommand}"/>
                    <MenuItem Header="Zoom Out" Command="{Binding ZoomOutCommand}"/>
                    <!-- Other View commands -->
                </MenuItem>
                <Separator Style="{StaticResource VerticalMenuSeparatorStyle}" />
                <MenuItem Width="24" Padding="10,5" Margin="2.5,0">
                    <MenuItem.Icon>
                        <Image Source="pack://application:,,,/Resources/save.ico" />
                    </MenuItem.Icon>
                </MenuItem>
                <Separator Style="{StaticResource VerticalMenuSeparatorStyle}" />
                <MenuItem Width="24" Padding="10,5" Margin="2.5,0">
                    <MenuItem.Icon>
                        <Image Source="pack://application:,,,/Resources/undoarrow.ico" />
                    </MenuItem.Icon>
                </MenuItem>
                <MenuItem Width="24" Padding="10,5" Margin="2.5,0">
                    <MenuItem.Icon>
                        <Image Source="pack://application:,,,/Resources/redoarrow.ico" />
                    </MenuItem.Icon>
                </MenuItem>
            </Menu>
            <!-- Tool Panel -->
            <StackPanel Orientation="Horizontal" Background="#333333">
                <!-- Tools Section -->
                <StackPanel Orientation="Vertical" Margin="10" >
                    <Button Padding="5" Margin="5" ToolTip="Spray Paint" Background="Transparent" Width="32" Height="32">
                        <Image Source="pack://application:,,,/Resources/spray_icon.ico" Height="32" Width="32"
                               HorizontalAlignment="Center" VerticalAlignment="Center" Stretch="Fill"/>
                    </Button>
                    <Button Padding="5" Margin="5" ToolTip="Eraser" Background="Transparent" Width="32" Height="32">
                        <Image Source="pack://application:,,,/Resources/eraser_icon.ico" Height="32" Width="32"
                               HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Button>
                    <TextBlock Text="Tools" Margin="5" Foreground="White" FontWeight="Normal" TextAlignment="Center" 
                               HorizontalAlignment="Center" VerticalAlignment="Center"/>
                </StackPanel>
                <Border Width="1" Margin="0, 10" Background="DarkGray" HorizontalAlignment="Center" />
                <!-- Size Section -->
                <StackPanel Orientation="Vertical" HorizontalAlignment="Center" Background="#333333" Margin="10">
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Center">
                        <Ellipse Width="5" Height="5" Fill="White" Margin="2"/>
                        <Slider Minimum="1" Maximum="50" Value="10" TickFrequency="1" Width="100" VerticalAlignment="Center"/>
                        <Ellipse Width="20" Height="20" Fill="White" Margin="2"/>
                    </StackPanel>
                    <TextBlock Text="Opacity" Foreground="White" FontWeight="Normal" TextAlignment="Center" />
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Center">
                        <Ellipse Width="5" Height="5" Fill="White" Margin="2"/>
                        <Slider Minimum="1" Maximum="50" Value="10" TickFrequency="1" Width="100" VerticalAlignment="Center"/>
                        <Ellipse Width="20" Height="20" Fill="White" Margin="2"/>
                    </StackPanel>
                    <TextBlock Text="Thickness" Foreground="White" FontWeight="Normal" TextAlignment="Center" />
                </StackPanel>

                <Border Width="1" Margin="0, 10" Background="DarkGray" HorizontalAlignment="Center" />
                <!-- Color Picker Section -->
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Top" Background="#333333">
                    <!-- Define a button for each color in your palette -->
                    <Button Width="30" Height="30" Margin="5" Background="Black" BorderBrush="Transparent" />
                    <Button Width="30" Height="30" Margin="5" Background="White" BorderBrush="Transparent" />
                    <Button Width="30" Height="30" Margin="5" Background="Red" BorderBrush="Transparent" />
                    <Button Width="30" Height="30" Margin="5" Background="Green" BorderBrush="Transparent" />
                    <Button Width="30" Height="30" Margin="5" Background="Blue" BorderBrush="Transparent" />
                    <Button Width="30" Height="30" Margin="5" Background="Yellow" BorderBrush="Transparent" />
                    <!-- Add more buttons for additional colors as needed -->
                </StackPanel>
            </StackPanel>
        </DockPanel>
        <!-- Canvas to draw on -->
        <Canvas Grid.Row="1" x:Name="PaintCanvas" Background="Transparent" ClipToBounds="True">
            <Image x:Name="EditableImage" Stretch="UniformToFill" />
        </Canvas>
    </Grid>
</Window>

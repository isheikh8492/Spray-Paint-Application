<Window x:Class="Spray_Paint_Application.View.PaintSpray"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:Spray_Paint_Application.ViewModel"
        Title="PaintSpray" Height="450" Width="800">
    <Window.DataContext>
        <local:SprayViewModel/>
    </Window.DataContext>
    <Window.InputBindings>
        <KeyBinding Command="{Binding UndoCommand}" Modifiers="Ctrl" Key="Z" />
        <KeyBinding Command="{Binding RedoCommand}" Modifiers="Ctrl" Key="Y" />
    </Window.InputBindings>
    <Grid>
        <Canvas Background="LightGray" MouseDown="Canvas_MouseDown" MouseMove="Canvas_MouseMove" MouseUp="Canvas_MouseUp">
            <ItemsControl ItemsSource="{Binding PaintDots}">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <Canvas IsItemsHost="True"/>
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
            </ItemsControl>
        </Canvas>
    </Grid>
</Window>
